/**
 * The GraphQL endpoint does not return a valid URL.
 * In their GitHub issues page people has posted a workaround by using a default base URL and adding the rest.
 */
export const spritesBaseURL =
  "https://raw.githubusercontent.com/PokeAPI/sprites/master/";
